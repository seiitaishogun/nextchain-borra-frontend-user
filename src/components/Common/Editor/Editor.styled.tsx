import styled from 'styled-components';

const EditorLayout = styled.div`
  .ck-editor__editable {
    min-height: 450px;

    h2 {
      font-size: 1.5em;
      font-weight: bold;
    }

    h3 {
      font-weight: bold;
      font-size: 1.17em;
    }

    h4 {
      font-weight: bold;
    }

    i {
      font-style: italic;
    }
  }
`;

export { EditorLayout };
