const enum SearchType {
  All = 'all',
  Name = 'name',
  Email = 'email',
  Phone = 'phone',
  Adid = 'ad_id',
}

const enum DateType {
  CreatedAt = 'created_at',
  UpdatedAt = 'updated_at',
}

export { SearchType, DateType };
